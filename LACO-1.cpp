#include <stdio.h>
#include <stdlib.h>

int main()
{
	for (int lapis = 1; lapis <= 100; lapis++) // for (variavel = valor da varial; condição; incrementador || decrementador)

		printf("%d\t", lapis);

		system("pause");
	    return (0);
}
